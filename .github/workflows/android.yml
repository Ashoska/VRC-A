name: Android CI

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"

      - name: Gradle cache
        uses: gradle/actions/setup-gradle@v3

      - name: Make gradlew executable
        run: chmod +x ./gradlew

      # Create signing files ONLY if secrets exist
      - name: Create signing files (if secrets set)
        shell: bash
        env:
          KEYSTORE_B64: ${{ secrets.KEYSTORE_B64 }}
          KEYSTORE_PASSWORD: ${{ secrets.KEYSTORE_PASSWORD }}
          KEY_ALIAS: ${{ secrets.KEY_ALIAS }}
          KEY_PASSWORD: ${{ secrets.KEY_PASSWORD }}
          RELEASE_PAT: ${{ secrets.RELEASE_PAT }}
          RELEASE_OWNER: ${{ secrets.RELEASE_OWNER }}
          RELEASE_REPO: ${{ secrets.RELEASE_REPO }}
        run: |
          set -e

          # Always write GitHub release credentials so admin build can push releases
          cat > keystore.properties <<EOF
githubPat=${RELEASE_PAT}
githubOwner=${RELEASE_OWNER}
githubRepo=${RELEASE_REPO}
EOF

          if [ -z "$KEYSTORE_B64" ]; then
            echo "No signing secrets found. Release will be unsigned."
            exit 0
          fi

          echo "$KEYSTORE_B64" | base64 -d > release.keystore

          # Append signing keys to the same file
          cat >> keystore.properties <<EOF
storeFile=release.keystore
storePassword=$KEYSTORE_PASSWORD
keyAlias=$KEY_ALIAS
keyPassword=$KEY_PASSWORD
EOF

          echo "Signing files created."

      # âœ… Build ONLY the variants we care about (separately)
      - name: Build Public Debug APK
        run: ./gradlew :app:assemblePublicAppDebug

      - name: Build Public Release APK
        run: ./gradlew :app:assemblePublicAppRelease

      - name: Build Admin Debug APK
        run: ./gradlew :app:assembleAdminAppDebug

      - name: Build Admin Release APK
        run: ./gradlew :app:assembleAdminAppRelease

      # âœ… Upload each APK as its own artifact so you can't install the wrong one
      - name: Upload Public Debug APK
        uses: actions/upload-artifact@v4
        with:
          name: public-debug
          path: app/build/outputs/apk/publicApp/debug/*.apk

      - name: Upload Public Release APK
        uses: actions/upload-artifact@v4
        with:
          name: public-release
          path: app/build/outputs/apk/publicApp/release/*.apk

      - name: Upload Admin Debug APK
        uses: actions/upload-artifact@v4
        with:
          name: admin-debug
          path: app/build/outputs/apk/adminApp/debug/*.apk

      - name: Upload Admin Release APK
        uses: actions/upload-artifact@v4
        with:
          name: admin-release
          path: app/build/outputs/apk/adminApp/release/*.apk
