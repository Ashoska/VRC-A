# (1) Server-initiated close: the bot must answer the close and RESUME right away (before the fix it
#     sat deaf ~50 s until a heartbeat went un-ACKed). Real Discord replays missed events on RESUME.
# (2) Non-recoverable close codes (4004 bad token, 4013/4014 intents — e.g. MESSAGE CONTENT not
#     enabled in the dev portal) must stop with FAILED and a plain reason (before the fix they were
#     retried forever showing only "Reconnecting…"). The admin then fixes the portal and presses
#     Start — simulated here by a config restart.
#
# Sessions opened after the first drop get a 4 s heartbeat so any retry loop would show within a minute.
@set ambient=0
> gateway-heartbeat 4000
> drop-gateway 4000
alice: @Cardinal you there?
> expect reply
> gateway-stats
> log
> gateway-reject 4014
> drop-gateway 4014
> sleep 20000
> gateway-stats
> log
> expect status FAILED
> gateway-accept
@set ambient=0
> sleep 3000
> gateway-stats
> log
bob: @Cardinal back?
> expect reply
